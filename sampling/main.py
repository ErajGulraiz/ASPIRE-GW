import matplotlib.pyplot as plt
import csv
import matplotlib.mlab as mlab
import numpy as np
import math
import random
from scipy import misc

from ranbh import *


from step1e import *# mass distribution from PDF for 100 random objects 

from step2 import *# finding out the R_gw as a function of z to get objects in each redshift bin

from allbins import* # module to produce the sample for dz=0.05 size and store it in different files named according to the redshift they are in..... it takes values from step2 module for the test array i.e number of objects in each redshift and uses it to loop for the probability distribution of those sample sizes.




def main():


	if __name__ == '__main__':
    		main()
